import Config

# config :oauth2_proxies_admin, docker_socket: "/var/run/docker.sock"
# config :oauth2_proxies_admin, docker_socket: "/run/podman/podman.sock"

# config :oauth2_proxies_admin, proxies_path: "/path/to/proxies"
